import { createTheme} from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
          main: '#332d75',
        },
        secondary: {
          main: '#d6902d',
        },
      },
})

export default theme